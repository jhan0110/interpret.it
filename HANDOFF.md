# Handoff — Interpretit Platform

State as of 2026-05-17. Phases 0–6 functionally complete on `worktree-phase5` (uncommitted). **Demo prep is mid-flight**: Phase B (cheap real-API verification) and Phase C (UI polish) are done. Phase D (single real-API n=10 end-to-end) is the only remaining step before the demo is gut-checkable in a real browser.

Total API spend so far: **~$0.07** (Phase B). Phase D will add ~$0.10. Single-digit cents to ship.

## TL;DR — what changed since last handoff

### Phase B — verified real-API path (~$0.07 spent)
- Cheap Claude call (n=2) produced two cohesive military-logistics phrases.
- Cheap ElevenLabs call returned a real 33 kB mp3 (mock would be ~5–10 kB silent).
- Full n=3 end-to-end generation completed (83 s cold start, 16 s warm) with real voiced audio at `tts/<hash>.mp3` keys.
- Quota override via `?force=1` worked; mocks reverted at the end.
- Caveat: arq's `job_timeout` was 120 s; cold-start (multilingual-e5 ~60 s + Claude + 3× TTS) ran 83 s and arq threw `KeyError` on cleanup *after* the DB write succeeded. **Patched** — `WorkerSettings.job_timeout` raised to 300 s in `services/analysis/app/worker.py`.

### Phase C-A — `SessionRunner.tsx` polish (zero API spend)
File scope: `frontend/app/(session)/SessionRunner.tsx` only. 251 → 531 lines. All six items shipped:

1. **Animated generation overlay** — progress bar with `transition-all`, small spinner, `scenario_summary` displayed for ~3 s after `generation.complete` then overlay fades.
2. **Segment progress indicator** — "Segment X of N" near the cognitive-load dot, hidden during `idle`. X comes from a local counter incremented on `segment.play`; N from `generation.target` when pre-generated, otherwise just X.
3. **Calibrated-delay countdown** — shrinking horizontal bar driven by `setInterval` while `listening`, sourced from `WSSegmentPlay.payload.delay_ms`. Record button disabled until 0.
4. **Recording level meter** — `AudioContext` + `AnalyserNode` reading from a second `getUserMedia` tap (the existing `AttemptRecorder.stream` is private; tap reuses identical constraints so the browser routes to the same device). Falls back to a pulsing dot if `getUserMedia` fails for the analyser. **Future cleanup**: add a `getStream()` accessor on `AttemptRecorder` in `lib/audio.ts` and have the meter use one stream — out of scope for this slice.
5. **End-of-session "View review" button** — `router.push(\`/review/${sessionId}\`)` when state hits `complete`.
6. **A11y on cognitive-load dot** — `aria-label` preserved + a sighted text label ("Load: low/moderate/high/overloaded") added.

Verified: `tsc --noEmit` clean, vitest 4/4 passing.

### Phase C-B — gateway + review page (zero API spend)
File scopes: `services/gateway/app/ws/session_socket.py`, `services/gateway/app/api/sessions.py`, `contracts/contracts.json`, `frontend/lib/contracts.ts`, `frontend/app/review/[sessionId]/page.tsx`.

1. **`delay_ms` now non-zero on `segment.play`** — `2000 + 500 * (difficulty_level - 1)`, clamped to `[2000, 6500]` ms. Lives in `_pick_and_emit_segment`.
2. **`GET /sessions/{session_id}/attempts/{attempt_id}/audio_url`** — new public REST endpoint in `app/api/sessions.py` (not `/internal/*`; browser calls it). Looks up the `AttemptRow`, mints a presigned URL via `signed_get_url(row.audio_path)`, returns `{audio_url, expires_in_s}`. Idempotent, read-only.
3. **`REST.GetAttemptAudioUrlResponse`** added to `contracts/contracts.json`; mirror in `frontend/lib/contracts.ts` (the TS file is now auto-generated by `scripts/gen-contracts.ts` — header warns "DO NOT EDIT MANUALLY", so future contract changes must edit JSON and re-run the generator).
4. **Review page audio playback** — `frontend/app/review/[sessionId]/page.tsx` parallel-fetches attempt audio URLs server-side via `Promise.all`, embeds `<audio controls preload="metadata">` above each "Your interpretation" section. 404 silently skips the audio element.

**Note**: C-B's subagent didn't run tests (no Bash permission in its sandbox). I haven't run them since either; running them is part of Phase D's preflight.

## What's still open (the demo path)

### Phase D — final n=10 real-API run (~$0.10, plus your browser drive)

1. **Preflight** — `pytest services/gateway/tests/ -x` and `cd frontend && npx tsc --noEmit && npx vitest run`. These haven't been re-run since C-A/C-B landed.
2. **Rebuild affected images** — gateway (sessions.py, session_socket.py), frontend (SessionRunner.tsx, page.tsx, contracts.ts).
3. **One n=10 real run** — `USE_MOCKS=0` on `arq-generation` via the temp `dc-override-real.yml` pattern Phase B used; `?force=1` on the POST to bypass quota; expect ~$0.10 (Claude ~$0.02, ElevenLabs ~$0.08).
4. **Verify** — `generation_state=ready`, `plan_n=10`, 10 cohesive English logistics phrases at internal levels in the L3 band, real voiced mp3s sized 30–80 kB each.
5. **Revert** — drop the override, `USE_MOCKS=1` back on, drop the temp file.
6. **Your part** — open Chrome, walk the dashboard → generated session → record one interpretation → land on review with audio playback. Tells you whether the demo holds together.

If you want repeat demos to cost ~$0, Phase D.3 (cache-reuse on session create — pool query before LLM call) is the cheapest follow-up.

## Demo automation plan — original (still applies for the remaining slice)

| Phase | Status | Estimated spend |
|---|---|---|
| A — keys + voice IDs | ✅ done by you | $0 |
| B.1 — cheap Claude (n=2) | ✅ ~$0.005 | |
| B.2 — cheap ElevenLabs | ✅ ~$0.005 | |
| B.3 — n=3 e2e | ✅ ~$0.05 | |
| B.4 — cache check | ✅ free | |
| B.5 — revert | ✅ — | |
| C-A — SessionRunner | ✅ $0 | |
| C-B — review + delay_ms | ✅ $0 | |
| **D.1 — n=10 real run** | **⏳ next** | ~$0.10 |
| D.2 — your browser drive | ⏳ needs you | $0 (no regen) |
| D.3 — cache-reuse (optional) | ⏳ | $0 |
| **Running total** | **~$0.07** | (D adds ~$0.10) |

## Quick orientation (unchanged)

- **What it is.** Real-time interpretation training for military/classified/research learners. Audio-only sessions, layered feedback.
- **Stack.** FastAPI gateway + FastAPI analysis (arq workers) + Next.js 16 frontend. Postgres 16 + pgvector + Redis (arq + pub/sub) + MinIO.
- **Contracts.** `contracts/contracts.json` is canonical. **TypeScript mirror is now auto-generated** (`scripts/gen-contracts.ts`) — edit JSON, run the generator. Pydantic mirror is still hand-maintained.
- **Working agreements.** `CLAUDE.md`. Specifically: no text during `(session)` route, gateway is single DB writer, content-generation parameters section, prompt-template-on-disk.

## Phase history

| Phase | Scope | State |
|---|---|---|
| 0 | Directive + `CLAUDE.md` | merged |
| 1 | `ARCHITECTURE.md` + contracts | merged |
| 2 | Gateway + Analysis services | merged |
| 3 | Frontend scaffold + integration tests | merged |
| 4 | Docker compose + perf + dashboard + seed | merged |
| 5 | Ladder picker + multi-segment + embeddings + paraphrase seeding | on `worktree-phase5` |
| 6 | Content generation (Claude+TTS pipeline, quota, WS progress, frontend params) | on `worktree-phase5` |
| Demo prep B | Real-API verification + arq timeout fix | on `worktree-phase5` |
| Demo prep C | UI polish (SessionRunner + review + delay_ms + audio-url endpoint) | on `worktree-phase5` |
| Demo prep D | n=10 real-API run + browser drive | **next** |

## Latest file delta (Phase B/C, since the previous handoff)

| File | Change |
|---|---|
| `services/analysis/app/worker.py` | `job_timeout` 120 → 300 s |
| `services/gateway/app/ws/session_socket.py` | `delay_ms` computed from difficulty in `_pick_and_emit_segment` |
| `services/gateway/app/api/sessions.py` | New `GET /sessions/{sid}/attempts/{aid}/audio_url` + response model |
| `contracts/contracts.json` | Added `REST.GetAttemptAudioUrlResponse` |
| `frontend/lib/contracts.ts` | Regenerated by `scripts/gen-contracts.ts` (auto-generated header now present) |
| `frontend/app/(session)/SessionRunner.tsx` | 6-item UX polish — 251 → 531 lines |
| `frontend/app/review/[sessionId]/page.tsx` | Per-attempt audio playback above "Your interpretation" |

## Conventions (unchanged)

- Worktrees for code changes (`.claude/worktrees/<name>`)
- One commit per logical change, Conventional Commits
- No emojis in code or commits
- Gateway is the only DB writer
- Audio blobs in MinIO, paths in Postgres
- No text during `(session)` route
- Prompt templates as Markdown on disk
- **Subagent orchestration** (per `interpretation-trainer-prompt.md`): Sonnet subagents with disjoint file scopes can run in parallel. Phase 2-style fan-out works well for UI polish (e.g. C-A + C-B). Sequential where steps depend on prior output (e.g. B.1 → B.2 → B.3).

## How to bring the stack up (unchanged)

```bash
cd /Users/jhyun/personal-projects/interpretit/.claude/worktrees/phase5
docker compose -p interpretit stop      # if your main repo's stack is running
docker compose up postgres redis minio -d
docker compose build gateway analysis
docker compose run --rm gateway alembic upgrade head
docker compose run --rm gateway python -m scripts.seed
docker compose build frontend          # regen lockfile in alpine if npm ci fails
docker compose up -d
```

## Recommended next agent action (Phase D)

Pre-flight (mock mode, $0):
- `pytest services/gateway/tests/ -x` — expect 38/38 green
- `cd frontend && npx tsc --noEmit && npx vitest run` — expect clean + 4 passing
- `docker compose up -d --build gateway frontend`

Real run (single Sonnet subagent, ~$0.10):
- Create `/tmp/dc-override-real.yml` with `USE_MOCKS: "0"` on `arq-generation`
- `docker compose -f docker-compose.yml -f /tmp/dc-override-real.yml up -d arq-generation`
- POST `/sessions?force=1` with `generation.n=10`, duration `medium`, topic `logistics`
- Poll until `generation_state=ready`
- Verify 10 phrases coherent, audio sizes plausible
- Revert override, `USE_MOCKS=1` back

After D.1, you drive Chrome (Phase D.2):
- <http://localhost:3000>
- Fill the form, watch the new animated overlay, click Begin, record at least one phrase, walk through to feedback, then "View review" → confirm audio playback works.

## Human-only actions remaining

- **Drive Phase D.2** (Chrome session walkthrough) — I can't drive a real browser.
- **Decide commit strategy** for `worktree-phase5` (now ~35 files modified or new). Single bundled commit vs split by slice (Phase 5 picker / Phase 6 generation / Phase B fixes / Phase C polish). I haven't committed.
- **Decide on D.3** (cache-reuse) — if you intend to demo more than once, the cache-query path makes repeats ~$0.

## Memory

`~/.claude/projects/-Users-jhyun-personal-projects-interpretit/memory/` — `feedback-pause-checklist` (end every pause with a human-only action list).

---

# Phase 7 — Learner-facing surface, Groq cutover, prosody simplification

State as of **2026-05-21**. Live and demonstrable end-to-end against real Claude + Groq + ElevenLabs APIs. Big-picture shift: the product now has a real learner-centric flow (login → home → train → vocab review) and the analysis pipeline collapsed from "two parallel workers" to "one semantic worker derives prosody from ASR word timestamps".

## What shipped since 2026-05-17

### Learner login + modular home hub (frontend)
- **`/login`** (`frontend/app/login/page.tsx`) — UUID input, validates existence via `GET /learners/{id}`, persists to `localStorage['interpretit:learner_id']`, redirects to home. Dev-learner prefill button.
- **`/learner/[learnerId]`** (`frontend/app/learner/[learnerId]/`) — server-rendered hub. Sections:
  - Greeting
  - Feature grid (hero) driven by `features.ts` registry — adding a new feature is one entry in that array
  - Overview: streak tile + minutes-interpreted tile + per-domain mastery bars
  - Recent sessions list
- **`/learner/[learnerId]/train`** — relocated session-creation form. Pre-fills learnerId from URL.
- **Root `/`** is now a client-side redirector to `/login` or `/learner/{id}` based on localStorage.
- **Back-to-home arrows** added on the train + vocab pages via shared `BackToHome.tsx`.

### Vocabulary deck with spaced repetition (full vertical slice)
- **DB (`0003_vocab_deck.py`)** — 3 new tables: `vocab_entries`, `learner_topics`, `learner_vocab_deck`. SM-2 algorithm fields (interval_days, ease_factor, repetitions, next_review_at).
- **Backend** — `services/gateway/app/api/vocab.py`, `app/vocab/seeds.py` (~30 curated terms × 6 domains), `app/vocab/seeder.py`, `app/vocab/srs.py` (SM-2 update).
- **Two SRS input signals**:
  1. Topic seeding — POST `/learners/{id}/vocab/topics` adds ~30 terms for that domain to the learner's deck.
  2. Extraction from failed interpretations — semantic worker enqueues `run_vocab_extraction` whenever `overall_score < 0.75` or errors include `lexical_gap`/`omission`. Claude classifies each missed term as `knowledge_gap` vs `memory_gap` and the gateway upserts it into `learner_vocab_deck` (already-present terms get their interval reset, re-surfacing the card).
- **Frontend** — `/vocab/[learnerId]` flashcard UI with flip + 4-grade SM-2 buttons (Again/Hard/Good/Easy). Topic chips to add/remove domains. Stats grid (due now / total / knowledge gaps / memory gaps).

### Overview backend
- **New endpoint** `GET /learners/{id}/overview` in `services/gateway/app/api/sessions.py` returns `{streak_days, total_seconds_interpreted, mastery_scores}`. Streak = consecutive UTC days ending today with ANY activity (attempt OR vocab review).
- **DB migration** `0004_attempt_duration.py` adds `attempts.duration_ms`; `session_socket.py` now persists `AudioSubmission.duration_ms` (previously discarded).

### ASR migration: faster-whisper → Groq
- New file `services/analysis/app/asr/transcribe_groq.py` — HTTP client for Groq's OpenAI-compatible `/audio/transcriptions` endpoint.
- `transcribe.py` is now a dispatcher on `WHISPER_PROVIDER=groq|local`. Default is `groq`.
- Eliminated the Whisper warm-up cold-start. Per-attempt timeout dropped 90s → 30s.
- New env: `GROQ_API_KEY`, `WHISPER_PROVIDER`, `GROQ_WHISPER_MODEL` (default `whisper-large-v3`).

### Prosody deprecation → word-timestamp derivation
- **Deleted** `services/analysis/app/prosody/pipeline.py` and the `arq-prosody` docker-compose service.
- **New** `services/analysis/app/prosody/word_prosody.py::compute_prosody_from_words` — pure-Python; takes the word-timestamp array Groq already returns and computes pause_count, silence_ratio, mean_wpm, filler_count, then runs the existing classifier. No silero-vad, no librosa, no torch.hub call.
- Prosody push now happens inside `run_semantic` right after Groq returns — cognitive-load dot appears in ~2s, restoring the original "fast path" UX without a second worker.
- Filler lexicon + cognitive_load classifier preserved unchanged.

### Dependency cleanup
Dropped from `services/analysis/pyproject.toml`: `faster-whisper`, `silero-vad`, `librosa`, `spacy`. Torch is now installed CPU-only via `--index-url https://download.pytorch.org/whl/cpu` (saves ~3 GB / image of NVIDIA CUDA libs). Kept: `pydub` (TTS disfluency mixing), `torch` (transitively required by `sentence-transformers`), `sentence-transformers` (paraphrase embeddings).

Net image impact: analysis went from ~5 GB → 2.91 GB.

### Dev infrastructure
- **Bind mounts** added in `docker-compose.yml` for `services/gateway/app`, `services/gateway/alembic`, `services/analysis/app`. Python source changes now require only `docker compose restart <service>` — no rebuild.
- **Image consolidation** — `analysis`, `arq-semantic`, `arq-generation` all share `phase5-analysis:latest` (one `build:` declaration, two `image:` references). Saves ~5.8 GB.

### Contract loosening (defensive)
- `AudioSubmission.audio_format` literal → `str` so Safari MP4/AAC recordings validate
- `AnalysisRequest.register` and `Segment.register` literal → `str` after Claude produced `register="operational"` from a generation prompt and blew up Pydantic validation, breaking attempt enqueue silently

### Frontend polish
- `<audio key={audioUrl}>` to force React to remount on src change (fixed "wrong audio playing" race)
- Two-phase progress bar during listen→record: 0–50% (blue, "Listening") tracks audio playback; 50–100% (yellow, "Recording in Ns") counts down post-audio delay. Record button locked until 100%.
- Synthetic 3-stage analyzing progress bar (Transcribing → Checking → Generating feedback). No real backend events yet — purely time-based against the ~12s SLA. Tagged debug logs (`[WS]`, `[audio]`, `[analyzing]`) throughout.

### Backend debug instrumentation
65 timestamped log statements across 9 files. Tag taxonomy: `[semantic.*]`, `[asr.groq.*]`, `[eval.claude.*]`, `[reference.claude.*]`, `[tts.*]`, `[rpc.push_*.*]`, `[gw.internal.*]`, `[gw.close_if_ready.*]`, `[ws.state_change.sent]`, `[prosody.derived]`. Each includes `attempt=<id>` so concurrent attempts can be greppped independently:
```bash
docker compose logs --since 5m arq-semantic gateway | grep '<attempt-id>'
```

## Current architecture quick-glance

```
Browser ──WS── Gateway ──Redis──> arq-semantic
                  │                    │
                  │                    ├── Groq /audio/transcriptions (ASR + words)
                  │                    ├── compute_prosody_from_words() → POST /internal/prosody_result
                  │                    ├── Redis (reference cache, 24h)
                  │                    ├── Claude (reference if miss)
                  │                    ├── ElevenLabs (feedback TTS)
                  │                    ├── Claude (evaluate) → POST /internal/semantic_result
                  │                    └── (conditional) enqueue run_vocab_extraction
                  └──> arq-generation (10-pack pre-gen, Claude + ElevenLabs)
```

## Known issues / followups

- Recent attempt records `b8d977a3`, `fb28b24c`, `506753db`, `106485e1`, `82930115` are permanently stuck with `closed_at=NULL` (failed mid-debug before fixes). Not user-visible; just DB cruft.
- `generation_state='operational'` register problem could recur with other non-canonical values. Long-term fix: clamp generator output in `services/analysis/app/content/session_generation.py` before insert. Currently we tolerate any string at the boundary.
- `GenerationParams.n` is set to **2** temporarily in `services/gateway/app/contracts/models.py` to save API spend during dev. Restore to `10` before demoing the full daily-training flow.
- Dev learner (`00000000-0000-0000-0000-000000000001`) has a hardcoded 100/day generation quota override in `services/gateway/app/quota.py`. Other learners use the default `GEN_QUOTA_DAILY=2`.
- Per-learner caps in `quota.py::PER_LEARNER_CAPS` is a dev affordance, not a designed feature. Move to DB or config before any real user enrollment.
- 5–8 stuck attempts in the DB from prosody/register bugs early in the session can be cleaned with a `DELETE FROM attempts WHERE closed_at IS NULL AND recorded_at < <cutoff>;` if you want a clean review page.

## Verification commands

```bash
# End-to-end smoke
curl http://localhost:8000/health                                                        # gateway
curl http://localhost:3000/login                                                          # frontend
docker compose ps                                                                         # all 8 containers up

# Trace an attempt
docker compose exec postgres psql -U interpretit -d interpretit -c \
  "SELECT id, prosody_result IS NOT NULL pros, semantic_result IS NOT NULL sem, closed_at FROM attempts ORDER BY recorded_at DESC LIMIT 3;"

# Tail key logs by tag
docker compose logs -f arq-semantic | grep -E '\[prosody.derived\]|\[asr.groq\.|\[eval\.|\[gw.close_if_ready'
```

---

# Phase 8 — Merge to main, three blocker fixes, feedback page, context-aware ASR

State as of **2026-05-22**. Phase 5/6/7 work merged into `main`. Three blocking session-flow bugs fixed (infinite analysis spinner, segment-count overrun, no feedback delivery). Feedback delivery redesigned from "deferred TTS audio" to a review-style **in-session text page**, with the `(session)` no-text rule amended to match. Context-aware ASR added (domain-vocabulary Whisper priming).

The four feature changes below are **uncommitted on `worktree-phase5`** on top of commit `6810cc7`. The merge itself is committed on `main`.

## What shipped since 2026-05-21

### Merge worktree-phase5 → main (committed)
- `worktree-phase5` HEAD bumped from `63abe05` to **`6810cc7` `feat(phase5): learner surface, content generation, Groq ASR pipeline`** — single commit consolidating ~79 files (~6,166 insertions): vocab deck, learner hub, content generation, segment picker, Groq ASR, schema migrations 0002–0004. Junk excluded (`.env.bak`, `docker-compose.yml.bak`, `frontend/frontend/`).
- Merged into `main` with `-X theirs` to honor "worktree is the verified build." `main` history now ends at **`43a4fe5` `fix: align merged files to the worktree build`** (after merge commit `ffb7761`). The superseded `services/analysis/app/embeddings/` package from `main`'s old `933d930` was removed in favor of the worktree's `embeddings.py` module. `git diff HEAD worktree-phase5` is empty — `main`'s tree matches the worktree's tree exactly at that point.
- `main`'s prior stray uncommitted edits (a defunct in-process `pubsub.py` analysis-fix attempt + ad-hoc docker edits) were discarded — superseded by the worktree's own versions and the proper fix below.

### Fix: infinite analysis spinner (`analyzing → feedback`)
- **Root cause**: `internal.py` persisted prosody/semantic results to the DB but the WebSocket layer was never notified. The state machine never transitioned out of `analyzing`, so the frontend's `state === "analyzing"` spinner gate (`SessionRunner.tsx:504`) never released. `internal.py`'s docstring openly admitted the WS fan-out was deferred "in a follow-up."
- **Fix** — Redis pub/sub mirroring the existing `_generation_listener` pattern (the generation flow already used a `generation_events` channel; we follow the same shape):
  - `services/gateway/app/api/internal.py`: `prosody_result` and `semantic_result` now publish a ready-to-send WS envelope on the new Redis `analysis_events` channel after persisting. `_close_if_ready` transitions the session `analyzing → feedback` (via `set_state`) once both pipelines close and publishes a `state.change` envelope. Guarded by `notify = session_row.state == "analyzing"` so a learner ending the session early isn't yanked out of `complete`.
  - `services/gateway/app/ws/session_socket.py`: new `_analysis_listener(ws, session_id)` — a near-copy of `_generation_listener` — subscribes to `analysis_events`, filters by `session_id`, forwards envelopes verbatim to the live socket. Started alongside `listener_task` and cancelled in the same `finally` block.
  - New constant `ANALYSIS_CHANNEL = "analysis_events"` co-located with `GENERATION_CHANNEL`.

### Fix: planned-session "Segment 3 of 2" overrun
- **Root cause**: two compounding issues.
  1. `session_socket.py` had `SESSION_SEGMENT_TARGET = 12` hardcoded, so a 2-phrase planned session wasn't `target_reached` until 12 segments.
  2. `pick_segment_for_session` fell through to the open-ended ladder picker once `_pick_from_plan` returned `None`, serving arbitrary extra segments from the general pool.
- **Fix**:
  - `services/gateway/app/engine/segment_picker.py`: planned sessions now return `_pick_from_plan(...)` directly — strictly bounded by their plan, no ladder fallthrough.
  - `services/gateway/app/session_manager.py`: `SessionSnapshot` gains `planned_count: int = len(row.planned_segment_ids or [])`.
  - `services/gateway/app/ws/session_socket.py`: `target_reached = snap.segment_count >= (snap.planned_count or SESSION_SEGMENT_TARGET)`. A 2-segment session cleanly hits `complete` at 2/2; open-ended (ladder-only) sessions still cap at `SESSION_SEGMENT_TARGET`.

### Feedback delivery: review-style page in `(session)`
- **Root cause**: the `feedback` state had no rendered content (just a `✓` glyph), and `SessionRunner.tsx:509` mounted the segment-audio `<audio autoPlay>` whenever state was `listening || feedback || next_segment` — so entering feedback **replayed the segment audio**. The original design intended TTS audio feedback but only the rendering hook was wired; nothing ever set `audioUrl` to a feedback URL, and under `USE_MOCKS=1` `feedback_audio_path` is a placeholder (`mocks/feedback_audio_placeholder.mp3`) anyway.
- **Design decision (user-approved)**: drop audio feedback for now; show a full review-style **text page** after each attempt. The "no text during sessions" rule is amended accordingly.
- **Fix**:
  - New `frontend/components/AttemptFeedback.tsx` — pure presentational component rendering semantic block (transcript / reference / score / errors with severity colors / feedback_text) + prosody metrics + optional attempt audio. Light-themed; render on a light background.
  - `frontend/app/review/[sessionId]/page.tsx` — refactored to use the shared component (~60 inline lines deleted; the `severityColor` helper moved into the component).
  - `frontend/app/(session)/SessionRunner.tsx` — captures `semantic.result` and `prosody.result` payloads into state on receipt, clears them on every `segment.play` (next attempt), renders `<AttemptFeedback>` inside a white card when `state === "feedback"`. Segment-`<audio>` gate narrowed from `listening || feedback || next_segment` to just `listening` — no more replay.
  - `CLAUDE.md` Key Architectural Rule: **"No text during sessions"** → **"No text while interpreting"**. Text remains forbidden during `listening`/`recording`; the `feedback` state now shows the full review breakdown. Compact Instructions reference updated to match.

### Context-aware ASR: domain-vocabulary priming
- **Why**: Whisper transcribed blind — both Groq and faster-whisper paths support a `prompt`/`initial_prompt` parameter for vocabulary biasing, neither used it. Technical jargon often got mis-spelled.
- **Scope decision (user-approved)**: prime with **domain vocabulary only** — *not* the reference answer or paraphrases. Biasing toward the correct answer would let a garbled interpretation transcribe as correct and mask the learner's real omissions/lexical-gap errors, defeating the evaluator. Domain seeds (jargon, proper nouns) help spell technical terms without revealing the answer.
- **Fix** (gateway builds the hint; analysis stays a pure consumer):
  - `services/gateway/app/vocab/seeds.py`: new pure helper `domain_asr_prompt(domain, target_lang) -> str` — comma-joins the domain's `TOPIC_SEEDS` terms in the spoken language (Korean `definition`s when `target_lang=="ko"`, English `term`s otherwise). Unknown domain → `""`.
  - `services/gateway/app/queue.py`: `enqueue_analysis` computes the hint (`empty → None`) and sets it on `AnalysisRequest.asr_prompt`.
  - New field on both `AnalysisRequest` Pydantic models (gateway `_Strict` + analysis `BaseModel`): `asr_prompt: str | None = None` — optional + defaulted, backward-compatible with in-flight arq payloads.
  - `contracts/contracts.json`: `AnalysisRequest` documented with the new optional field.
  - `services/analysis/app/asr/transcribe.py`: `transcribe(audio_path, lang, prompt=None)`; local path passes `initial_prompt=prompt` to `model.transcribe(...)`.
  - `services/analysis/app/asr/transcribe_groq.py`: Groq request `data` dict gains `"prompt"` only when truthy.
  - `services/analysis/app/worker.py`: both `transcribe` calls (cache-hit + parallel-gather branches, via `asyncio.to_thread`) forward `prompt=req.asr_prompt`.
  - `services/gateway/tests/test_asr_hints.py`: 7 tests covering known domains × `ko`/`en` and unknown-domain fallback. **All passing locally.**
- **Effect**: zero behavior change under `USE_MOCKS=1` (the mock pipeline never calls `transcribe`). With real Groq ASR (`WHISPER_PROVIDER=groq`), the API request now carries a ~30-term domain hint string under Whisper's ~224-token prompt window.

### Operational reset (no code change)
- Running stack briefly diverged from `docker-compose.yml`: an orphaned `arq-generation` container survived a service rename and held 788 MiB RAM + 2.26 GB of HF-model writable layer. A stale `interpretit-*` project (pre-rename) had 3 exited containers + 2 dead volumes. All removed. `docker compose -p phase5` is now the canonical project; running set matches compose.

## File delta since 2026-05-21 (uncommitted on worktree-phase5)

| File | Change |
|---|---|
| `services/gateway/app/api/internal.py` | Redis publish on `analysis_events`; state transition `analyzing → feedback` in `_close_if_ready` |
| `services/gateway/app/ws/session_socket.py` | `_analysis_listener` (Redis subscriber); `target_reached` from `planned_count`; segment audio gate narrowed |
| `services/gateway/app/session_manager.py` | `SessionSnapshot.planned_count` |
| `services/gateway/app/engine/segment_picker.py` | Planned-session strictly bounded; no ladder fallthrough |
| `services/gateway/app/vocab/seeds.py` | `domain_asr_prompt` helper |
| `services/gateway/app/queue.py` | `enqueue_analysis` sets `asr_prompt` from the helper |
| `services/gateway/app/contracts/models.py` | `AnalysisRequest.asr_prompt` (optional) |
| `services/gateway/tests/test_asr_hints.py` | **new** — 7 tests, all green |
| `services/analysis/app/contracts/models.py` | `AnalysisRequest.asr_prompt` (optional) |
| `services/analysis/app/asr/transcribe.py` | `prompt` param threaded; `initial_prompt=` to faster-whisper |
| `services/analysis/app/asr/transcribe_groq.py` | `data["prompt"] = prompt` when set |
| `services/analysis/app/worker.py` | Both `transcribe` calls forward `prompt=req.asr_prompt` |
| `frontend/components/AttemptFeedback.tsx` | **new** — shared review-style attempt card |
| `frontend/app/review/[sessionId]/page.tsx` | Uses `AttemptFeedback`; inline render + `severityColor` removed |
| `frontend/app/(session)/SessionRunner.tsx` | Result-payload state; feedback page in `feedback` state; audio gate narrowed |
| `CLAUDE.md` | "No text during sessions" → "No text while interpreting" (Key Rule + Compact Instructions) |
| `contracts/contracts.json` | `AnalysisRequest.asr_prompt` documented |

## Known issues / followups (in addition to Phase 7's)

- **All Phase 8 changes are uncommitted on `worktree-phase5`.** Decide commit granularity before the next merge to `main` — natural slices: (a) spinner fix, (b) segment-count fix, (c) feedback page + CLAUDE.md amendment, (d) ASR vocab priming. The repo's "one commit per logical change" rule prefers four commits over one squash.
- **Feedback-audio TTS is gone, not deferred.** The `feedback_audio_path` on `ProsodyResult`/`SemanticResult` is still emitted (placeholder under mocks) but the frontend no longer attempts to play it. If audio feedback returns later, it needs (i) gateway-signed URL on the WS envelope, (ii) real TTS upload to MinIO, (iii) a player wired into the feedback state.
- **Personal vocab deck is not in the ASR prompt.** Only the static domain seeds are. Adding the learner's `learner_vocab_deck` terms (terms they've missed) would be a small extension — one extra DB query in the gateway's `enqueue_analysis`, joined with the seed terms before passing to `domain_asr_prompt`.
- **No real-mode verification of the ASR prompt yet.** Needs `USE_MOCKS=0` + `GROQ_API_KEY`; speak a domain term that Whisper might mis-hear, confirm correct transcription and that the Groq request log carries `prompt=...`.
- **Frontend rebuild required for any UI change** (no bind mount). Backend changes (`services/gateway/app`, `services/analysis/app`) are bind-mounted — `docker compose restart <service>` suffices.

## Verification commands (Phase 8)

```bash
# Backend syntax + unit test (host Python is fine — gateway has no async deps in the helper)
python3 -m py_compile $(git -C .claude/worktrees/phase5 diff --name-only --diff-filter=AM -- '*.py')
python3 -m pytest services/gateway/tests/test_asr_hints.py -q   # 7 passed

# Confirm services healthy with new code
curl -s -o /dev/null -w "gateway:%{http_code} " http://localhost:8000/health
curl -s -o /dev/null -w "analysis:%{http_code}\n" http://localhost:8001/health

# Spot-check the Redis analysis_events flow during a session
docker compose exec redis redis-cli PSUBSCRIBE 'analysis_events'

# Confirm planned-session bound: complete a 2-segment session and see state hit `complete` at 2/2
docker compose exec postgres psql -U interpretit -d interpretit -c \
  "SELECT state, segment_count, array_length(planned_segment_ids, 1) AS planned FROM sessions ORDER BY started_at DESC LIMIT 3;"
```

## Human-only actions remaining

- **Commit + merge the four Phase 8 changes** to `main` once you've smoke-tested the session flow end-to-end in Chrome.
- **Real-mode ASR sanity check** with `USE_MOCKS=0` + `GROQ_API_KEY` if/when you want to confirm the vocab biasing actually improves jargon transcription.
- **Decide on feedback audio** — is the text-only feedback page final, or do you want the TTS audio path completed later?
- **Decide on personal-deck-in-ASR-prompt** — feed the learner's missed terms into the Whisper hint, not just the domain seeds.
